<% if current_member.following?(member) %>
  <%= link_to "フォロー中", member_relationships_path(member.id), method: :delete, remote: true, class: "btn btn-outline-primary" %>
<% else %>
  <%= link_to "フォローする", member_relationships_path(member.id), method: :post, remote: true, class: "btn btn-primary" %>
<% end %>