<% if current_member.email == "guest@example.com" %>
  <p>メンバー登録をしてコメントしましょう！</p>
<% else %>
  <%= form_with model: [photo, comment], local: false do |f| %>
    <%= f.text_area :comment, placeholder: "コメントを入力" %>
    <%= f.submit '送信' %>
  <% end %>
<% end %>